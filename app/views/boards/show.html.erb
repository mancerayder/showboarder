<%= @board.name %>
<%= render 'shared/new_show_button' %>

<% if current_user.boarder?(@board) %>
<%= current_user.board_role(@board) %>
yay!
<% else %>
boooooo!
<% end %>

<%= render @shows %>
<%= will_paginate @shows %>