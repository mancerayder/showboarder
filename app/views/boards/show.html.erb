<%= @board.name %>
<br>
<%= @board.stages.first.places_formatted_address_short %>
<% if user_signed_in? %>
<% if current_user.boarder?(@board) %>
<%= render 'shared/new_show_button' %>
<%= link_to image_tag('stripe-connect-button.png',:height=>33), "https://connect.stripe.com/oauth/authorize?response_type=code&client_id=ca_3wyuvdI1UcObVuCyRPLURKjmKYXABgFs" %>
<% if not @board.paid %>
<% end %>

<br>
<% if user_signed_in? %>
role <%= current_user.board_role(@board) %>
<% end %>
<br>
capacity <%= @board.stages.first.capacity %>
<br>


<%= render 'enable_ticketing_button'%>

boarder
<% else %>
not boarder
<% end %>
<br>
<% end %>
<%= @board.stages.first.places_reference %>
<br>
<%= @board.stages.first.places_json %>
<%= render @shows %>
<%= will_paginate @shows %>