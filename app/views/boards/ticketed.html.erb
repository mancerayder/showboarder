Enable fee-free ticketing on all shows on this Showboard for a flat rate of $25/month

<%= render 'sales/checkout_form', :object_path => board_ticketed_path(@board), :sale => @sale, :amount => @amount, :coupon => @coupon %>