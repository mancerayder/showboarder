<!-- <%= link_to "Enable ticketing for this Showboard", board_subscribe_path(@board) %> -->
<!-- <%= form_tag board_show_tickets_path(@board, @show) %>
  <%= number_field_tag "quantity", params[:quantity] %>
<%= submit_tag "Buy Tickets", :class => "blue_button disabled", :id => "checkout_button" %> -->

<%= form_tag board_show_tickets_path(@board, @show, :charge_type => "sb3") do %>
  <article>
    <label class="amount">
      <span>Purchase <%= @quantity %> tickets for <%= @amount %></span>
    </label>
  </article>

  quantity: <%= number_field_tag "quantity", params[:quantity] %>

  <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
          data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
          data-description="A month's subscription"
          data-amount=@amount></script>
<% end %>