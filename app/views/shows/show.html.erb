<h1><%= @acts_stringed + " at #{@show.board.name}"  %></h1>
<h2><%= @show.show_at.to_time.strftime('%B %e at %l:%M %p') %></h2>

<%= link_to "Back to #{@show.board.name}", @show.board %>
<br>
<% if user_signed_in? %>
<% if current_user.boarder?(@show.board) %>
<%= render 'enable_ticketing_button' %>
<% end %>

<% end %>
<br>

<% if @show.ticketing_type == "paid" %>
  <%= render 'reserve_tickets_button' %>
<% elsif @show.ticketing_type == "pwyw" %>
<% elsif @show.ticketing_type == "rsvp" %>
<% else %>
This show is free!
<% end %>

<%= render @show.acts %>

<br>
<%= number_to_currency(@show.price_adv, unit:"$") %>

Door time is <%= @show.door_at.in_time_zone(@show.board.timezone).strftime("%B %d, %Y at %l:%M %p") %>
<br>
Show time is <%= @show.show_at.in_time_zone(@show.board.timezone).strftime("%B %d, %Y at %l:%M %p") %>